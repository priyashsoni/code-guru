#include <stdio.h>
int main()
{
    int n, n2, max, sum, min, count, x;
    printf("enter a number");
    scanf("%d", &n);
    while (n != 0)
    {
        if ((n % x == 0) && (n % n2 == 0))
            printf("%d\n%d\n", x, n2);
        }

    return 0;
}