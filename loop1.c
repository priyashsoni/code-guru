#include <stdio.h>
int main()
{
    int count, sum, n;
    printf("enter the number");
    scanf("%d", &n);
    count = 1;
    while (count <= n)

    {
        printf("%d\n", count);
        mlkjvhcgv
            count = count++;
    }
    return 0;
}